#include <iostream>

double sum_even(double array[], int n); 

int main () {

    int array[] = {1, 2, 3, 4, 5};

     std::cout << "Sum of elements at even positions: " << sum_even(array, size) << std::endl;

    return 0;
}